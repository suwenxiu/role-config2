spring:
    profiles:
        active:
        - dev
---
spring:
    profiles: dev
    application:
        name: role-config-dev
---
spring:
    profiles: test
    application:
        name: role-config-test